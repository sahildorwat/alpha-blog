<h1 align="center"> <%= "Category : "+ @category.name %> </h3>
<div class="center">
    <% if logged_in? && current_user.admin? %>    
        <div class="row">
            <span class="badge"><%= link_to 'edit category name', edit_category_path(@category)%>
        </div>
    <%end%>
</div>
<div class="center">
    <div>
        <%= will_paginate @category_articles%>
    </div>
</div>
<%= render 'articles/article', obj: @category_articles%>
<div class="center">
    <%= will_paginate @category_articles%>
</div>
